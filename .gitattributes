# Enforce LF line endings for all text files in this repository.
# This prevents the "LF will be replaced by CRLF" Git warnings on Windows
# and keeps line endings consistent across all contributors and CI environments.
* text=auto eol=lf
